apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: saas-api-gateway
  annotations:
    alb.ingress.kubernetes.io/listen-ports: '[{"HTTP":80},{"HTTPS":443}]'
    alb.ingress.kubernetes.io/ssl-redirect: "443"
    alb.ingress.kubernetes.io/certificate-arn: arn:aws:acm:eu-central-1:123456789012:certificate/STAGING-CERT-ARN
    alb.ingress.kubernetes.io/group.name: platform-staging
spec:
  rules:
    - host: app.staging.example.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: saas-api-gateway
                port:
                  name: http
