apiVersion: v1
kind: ServiceAccount
metadata:
  name: ai-troubleshooter
  namespace: ai-troubleshooter
  labels:
    app: ai-troubleshooter
  # No IRSA annotation needed: the Anthropic API key arrives via ExternalSecret
  # (ESO uses its own IRSA role to fetch from Secrets Manager).
automountServiceAccountToken: true
